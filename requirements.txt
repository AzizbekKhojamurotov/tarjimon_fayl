# Core Telegram bot framework (async, aiogram v3)
aiogram==3.13.1

# DOCX manipulation — python-docx for reading/writing Word files
python-docx==1.1.2

# Online translation engine (Google Translate via deep-translator)
deep-translator==1.11.4

# Load BOT_TOKEN from a .env file during development
python-dotenv==1.0.1
